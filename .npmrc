node-linker=hoisted

# Enable build scripts for packages that need native binaries
allowBuilds[]=esbuild
allowBuilds[]=sharp
allowBuilds[]=unrs-resolver
allowBuilds[]=msw

# Allow all build scripts (pnpm will still respect lockfile unless overridden)
ignore-scripts=false