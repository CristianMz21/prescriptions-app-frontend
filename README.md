# Prescriptions App Frontend

A Next.js 15 application for managing medical prescriptions, built with the Rx-OS design system.

## Tech Stack

- **Framework**: Next.js 15 (App Router)
- **Package Manager**: pnpm
- **API Client**: Orval (generated from OpenAPI spec) + Axios + TanStack Query
- **Styling**: Rx-OS design system with CSS Modules

## Getting Started

```bash
# Install dependencies
pnpm install

# Copy environment config
cp .env.local.example .env.local

# Start development server
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

## Scripts

| Command | Description |
|---------|-------------|
| `pnpm dev` | Start development server (port 3000) |
| `pnpm build` | Build for production |
| `pnpm start` | Serve production build |
| `pnpm lint` | Run ESLint |
| `pnpm typecheck` | Run TypeScript type checking |
| `pnpm test` | Run Vitest tests |
| `pnpm api:refresh` | Regenerate API client from backend OpenAPI spec |

## Project Structure

```
src/
├── app/                    # Next.js App Router pages
│   ├── (auth)/            # Authentication pages (login)
│   ├── admin/             # Admin dashboard & metrics
│   ├── doctor/            # Doctor prescription management
│   └── patient/           # Patient prescription views
├── components/            # Reusable UI components
├── lib/
│   ├── api/               # API client (generated + custom)
│   ├── auth/              # Server-side auth utilities
│   └── hooks/             # Client-side React hooks
```

## Environment Variables

| Variable | Description | Default |
|----------|-------------|---------|
| `NEXT_PUBLIC_API_URL` | Backend API URL | `http://127.0.0.1:3000` |

## Architecture

This frontend consumes a NestJS backend API via an OpenAPI-generated client. See the root `ARCHITECTURE.md` for full system design.

### API Layer

The SDK in `src/lib/api/generated/` is generated by Orval from `backend/openapi.json`. Never edit these files directly — run `pnpm api:refresh` when the backend spec changes.

### Authentication

Uses HTTP-only session cookies. Auth is handled differently per context:
- **Server Components**: `src/lib/auth/server.ts` (`getAuth()`, `requireAuth()`)
- **Client Components**: `src/lib/hooks/useAuth.tsx` hook with `AuthProvider`

## Learn More

- [Next.js Documentation](https://nextjs.org/docs)
- [Rx-OS Design System](./src/app/globals.css)
